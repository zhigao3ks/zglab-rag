# Public API base URL.
# Empty string (default) means same-origin: /api/v1/ask/stream.
# Local development uses the Vite dev proxy (/api -> http://127.0.0.1:8000),
# so this normally stays empty. Production (Phase 10) is same-origin Nginx.
VITE_API_BASE_URL=
